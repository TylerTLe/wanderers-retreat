/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true,
    }
    images: {
        domains: [
            "ava"
        ]
    }
}

module.exports = nextConfig
