"use client";

interface InputProps {
    id: string;
    label: string;
    type?: string;
    disabled?: boolean;
    formartPrice?: boolean;
    required?: boolean;
    register:
}

const Input = () => {
  return (
  <div>

  </div>
  );
}

export default Input;
