/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true,
    }
    images: {
        domains: [
            "avata"
        ]
    }
}

module.exports = nextConfig
